apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mssql-pvc
  namespace: cemp
spec:
  accessModes:
        - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 5Gi