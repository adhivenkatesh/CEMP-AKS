apiVersion: v1
kind: Service
metadata:
  name: cemp-api-service
  namespace: cemp
spec:
  selector:
    app: cemp-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
