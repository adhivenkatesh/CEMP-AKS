apiVersion: v1
kind: Service
metadata:
  name: cemp-api-service
  namespace: cemp
spec:
  type: LoadBalancer
  ports:
    - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: cemp-api
