apiVersion: v1
kind: Service
metadata:
  name: cemp-api-service
  namespace: cemp
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
    targetPort: 8080
  selector:
    app: cemp-api