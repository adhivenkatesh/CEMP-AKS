apiVersion: apps/v1
kind: Deployment
metadata:
  name: cemp-api
  namespace: {{ .Release.Namespace }}
  labels:
    app: cemp-api
    release: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: cemp-api
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: cemp-api
        release: {{ .Release.Name }}
    spec:
      containers:
      - name: cemp-api
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "{{ .Values.env.ASPNETCORE_ENVIRONMENT }}"
        - name: DB_SERVER
          value: "{{ .Values.env.DB_SERVER }}"
        - name: DB_NAME
          value: "{{ .Values.env.DB_NAME }}"
        - name: DB_USER
          value: "{{ .Values.env.DB_USER }}"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mssql-secret
              key: SA_PASSWORD
        - name: BlobStorage__ConnectionString
          value: "{{ .Values.env.BlobStorage__ConnectionString | quote }}"
        - name: ConnectionStrings__BlobStorage
          value: "{{ .Values.env.ConnectionStrings__BlobStorage | quote }}"
        - name: AZURE_STORAGE_CONNECTION_STRING
          value: "{{ .Values.env.AZURE_STORAGE_CONNECTION_STRING | quote }}"
        - name: ConnectionStrings__DefaultConnection
          value: "{{ .Values.env.ConnectionStrings__DefaultConnection | quote }}"