apiVersion: v1
kind: Service
metadata:
  name: cemp-api-service
  namespace: cemp
spec:
  type: LoadBalancer
  selector:
    app: cemp-api
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
       
